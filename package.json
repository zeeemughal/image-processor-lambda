{
  "name": "image-processor-lambda",
  "version": "1.0.0",
  "private": true,
  "description": "S3-event Lambda: compresses uploaded images with Sharp and notifies the backend API",
  "main": "src/handler.js",
  "scripts": {
    "test": "node test/local.test.js"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.741.0",
    "@aws-sdk/client-sqs": "^3.1121.0"
  },
  "optionalDependencies": {
    "sharp": "^0.33.5"
  },
  "engines": {
    "node": ">=20"
  }
}
